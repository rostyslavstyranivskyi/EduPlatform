package com.eduplatform.ui.theme

import androidx.compose.ui.graphics.Color

// ── Ledger Palette ──────────────────────────────────────────────────────────
// Взято з веб-сайту (index.css @theme)

// Ink — основний темний колір (текст, фон кнопок)
val Ink          = Color(0xFF14213D)
val InkLight     = Color(0xFF2A3A63)

// Paper — фоновий колір (як білий аркуш паперу)
val Paper        = Color(0xFFFBF8F2)
val PaperRaised  = Color(0xFFFFFFFF)

// Gold — акцентний/primary колір (CTA кнопки, виділення)
val Gold         = Color(0xFFE3A33E)
val GoldDark     = Color(0xFFC4842A)

// Amber — secondary акцент, теплий оранжевий відтінок (замість Teal)
val Amber        = Color(0xFFEF8A3D)
val AmberDark    = Color(0xFFD16E22)

// Tangerine — tertiary акцент, ще один оранжевий відтінок (теплий, з нахилом у червоний)
val Tangerine     = Color(0xFFF77A3C)
val TangerineDark = Color(0xFFD15D22)

// Teal — залишено для сумісності, більше не використовується в темі
val Teal         = Color(0xFF2F7B6E)
val TealDark     = Color(0xFF225A51)

// Coral — небезпека / помилка
val Coral        = Color(0xFFD9624B)
val CoralDark    = Color(0xFFB94C38)

// Slate — вторинний текст (підписи, плейсхолдери)
val Slate        = Color(0xFF6B7280)

// Line — роздільники, бордери
val Line         = Color(0xFFE5E0D5)
